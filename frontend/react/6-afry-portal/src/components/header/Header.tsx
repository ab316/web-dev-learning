import BottomBar from './BottomBar';

import Topbar from './topbar/Topbar';

const Header = () => {
  return (
    <header>
      <Topbar />
      <BottomBar />
    </header>
  );
};

export default Header;
